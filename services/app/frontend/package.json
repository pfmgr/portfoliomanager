{
  "name": "portfolio-admin-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "25.3.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vitest run --coverage && vite build",
    "preview": "vite preview",
    "test": "vitest run --coverage",
    "test:unit": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:running-stack": "./scripts/run-running-stack-e2e.sh"
  },
  "dependencies": {
    "vue": "^3.5.31",
    "vue-router": "^5.0.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitejs/plugin-vue": "^6.0.5",
    "@vitest/coverage-v8": "^4.1.2",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^29.0.1",
    "vite": "^8.0.3",
    "vitest": "^4.0.18"
  }
}
