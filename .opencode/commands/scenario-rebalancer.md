---
description: Load the Rebalancer scenario documentation
---

Use `@scenarios/rebalancer.md` as the canonical feature scenario.
Summarize the current behavior, key endpoints, data dependencies, and implementation touchpoints relevant to the user request.
Highlight the verification skills referenced in that scenario, especially `scenario-lifecycle`, `backend-junit-tests`, `frontend-vitest-tests`, `frontend-e2e-tests`, and `running-instance-smoke-tests`.
